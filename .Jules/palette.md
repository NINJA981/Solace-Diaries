## 2026-05-31 - Missing ARIA Labels on Icon-only Buttons
**Learning:** Found a recurring pattern where icon-only buttons (like mobile menu, profile toggle, password visibility toggle, edit/delete entry buttons) lack `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Always ensure icon-only buttons have descriptive `aria-label` attributes for screen reader accessibility.
